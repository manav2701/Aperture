packages:
  - apps/*
  - packages/*

allowBuilds:
  esbuild: true

# Supply-chain defence: don't install versions published less than 3 days ago
# (the LiteLLM and axios compromises of March 2026 were pulled within hours).
minimumReleaseAge: 4320
