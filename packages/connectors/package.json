{
  "name": "@aperture/connectors",
  "version": "0.0.0",
  "private": true,
  "description": "Provider connectors (OpenRouter, OpenAI, Anthropic, Google, Hugging Face) and the model price catalog",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./test/fake-fetch.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@aperture/config": "workspace:*",
    "fast-check": "^4.10.2"
  }
}
